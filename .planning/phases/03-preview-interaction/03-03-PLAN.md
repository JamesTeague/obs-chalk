---
phase: 03-preview-interaction
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified: []
autonomous: false
requirements: [PREV-01, PREV-02, PREV-03, PREV-04, INPT-03]

must_haves:
  truths:
    - "All 5 phase requirements verified in live OBS"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 3 requirements in OBS with the built and installed plugin.

Purpose: Every phase requirement (PREV-01 through PREV-04, INPT-03) must be manually verified in OBS before the phase is marked complete. This is the only way to confirm correct behavior — there is no automated test harness for OBS plugin UI interactions.

Output: User confirms all requirements pass, or describes issues for gap closure.
</objective>

<execution_context>
@/Users/jteague/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jteague/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-preview-interaction/03-01-SUMMARY.md
@.planning/phases/03-preview-interaction/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and install plugin</name>
  <files></files>
  <action>
Build the plugin and copy to OBS plugin directory:
```bash
cd /Users/jteague/dev/obs-chalk
cmake --preset macos
cmake --build .build/macos -- -j4
cp .build/macos/obs-chalk.plugin/Contents/MacOS/obs-chalk \
   /Applications/OBS.app/Contents/PlugIns/obs-chalk.plugin/Contents/MacOS/obs-chalk
```
Verify build succeeds with no errors. If there are warnings, note them but proceed if build completes.
  </action>
  <verify>
    <automated>cd /Users/jteague/dev/obs-chalk && cmake --build .build/macos -- -j4 2>&1 | tail -3</automated>
  </verify>
  <done>Plugin binary built and copied to OBS plugin directory.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all Phase 3 requirements in OBS</name>
  <files></files>
  <action>
Present the verification checklist to the user and wait for confirmation. This is a human-verify checkpoint — all work was automated in Plans 01 and 02.

What was built: Complete Phase 3 preview interaction — Qt event filter on OBS preview, coordinate mapping, chalk mode hotkey, cursor, tablet pressure sensitivity.
  </action>
  <verify>User confirms all checks pass or describes issues.</verify>
  <done>All 5 phase requirements verified in live OBS by human tester.</done>
  <what-built>Complete Phase 3 preview interaction: Qt event filter on OBS preview, coordinate mapping, chalk mode hotkey, cursor, tablet pressure sensitivity.</what-built>
  <how-to-verify>
Launch OBS and verify each requirement:

**PREV-03 — Global hotkey toggle:**
1. Open OBS > Settings > Hotkeys
2. Find "Chalk: Toggle Chalk Mode" — bind it to a key (e.g., F9)
3. Press the hotkey — should toggle chalk mode

**PREV-04 — Custom cursor:**
4. With chalk mode ON, preview cursor should be a crosshair
5. With chalk mode OFF, cursor should be normal (default OBS cursor)

**PREV-01 — Event filter intercepts mouse events:**
6. With chalk mode ON, click and drag on the OBS preview — should create marks
7. With chalk mode OFF, click on preview — should select/move sources normally (no marks created)

**PREV-02 — Coordinate mapping:**
8. With chalk mode ON, draw a mark in the top-left corner of the canvas — it should appear at the top-left, not offset
9. Draw at the bottom-right corner — should appear at the bottom-right
10. Resize the OBS preview window, draw again — marks should still appear at the correct positions (coordinate mapping adjusts for scale)

**INPT-03 — Tablet pressure (if tablet available):**
11. With chalk mode ON and freehand tool selected, draw with a tablet pen
12. Light pressure should produce thin strokes
13. Heavy pressure should produce thick strokes
14. Varying pressure mid-stroke should show visible width changes

**Regression checks:**
15. All existing tools work (freehand, arrow, circle, cone)
16. Undo, clear-all, color cycle hotkeys still work
17. Laser pointer still works
18. Source interaction mode still works as fallback (right-click source > Interact, then draw)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. Otherwise describe which requirements failed and how.</resume-signal>
</task>

</tasks>

<verification>
All 5 phase requirements verified by human in live OBS environment.
</verification>

<success_criteria>
- User confirms PREV-01: event filter intercepts when chalk mode on
- User confirms PREV-02: marks at correct positions
- User confirms PREV-03: global hotkey toggles chalk mode
- User confirms PREV-04: cursor changes
- User confirms INPT-03: tablet pressure varies stroke width (or documented as untestable if no tablet available)
- No regressions in existing Phase 1-2 functionality
</success_criteria>

<output>
After completion, create `.planning/phases/03-preview-interaction/03-03-SUMMARY.md`
</output>
